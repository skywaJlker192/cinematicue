# BACKUP_RESTORE_REPORT.md — Отчёт о резервном копировании Cinematheque

## 1. Что резервируется

| Тип данных | Путь | Частота | Метод |
|------------|------|---------|-------|
| База данных (заказы, пользователи) | `backend/db.json` | Перед каждым деплоем / вручную | Копирование файла |
| Конфигурация | `.env`, `docker-compose.prod.yml` | При изменении | Версионирование в Git |
| Логи | `logs/*.log` | Еженедельно | Архивация |

## 2. Создание бэкапа

### Команда:
```bash
scripts\backup.bat
